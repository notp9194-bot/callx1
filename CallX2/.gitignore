# ── Credentials & Secrets (NEVER commit these) ─────────────────────────
# FIX [P4-1]: google-services.json contains Firebase API key + project ID.
# Each developer / CI environment must supply their own copy.
google-services.json
*.keystore
*.jks
release.keystore
upload.keystore
keystore.properties
local.properties

# ── Build artifacts ───────────────────────────────────────────────────────
*.apk
*.aab
*.dex
build/
.gradle/
.idea/
out/
*.class

# ── Environment / IDE ─────────────────────────────────────────────────────
.env
.env.*
*.iml
.DS_Store
Thumbs.db
captures/

# ── Proguard mapping (keep for crash symbolication — store externally) ────
mapping.txt
seeds.txt
usage.txt
