<?xml version="1.0" encoding="utf-8"?>
<!-- Reel Editor step-card (screenshot-style icon boxes) — CURRENT step.
     Gradient "border" is faked the same way bg_step_indicator_pill does:
     an outer gradient-filled rounded rect, inset by a solid dark rounded
     rect on top, leaving a ~2dp gradient ring visible around the edge. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="45"
                android:startColor="@color/trim_gradient_start"
                android:endColor="@color/trim_gradient_end"/>
            <corners android:radius="16dp"/>
        </shape>
    </item>

    <item
        android:top="2dp"
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/trim_card_bg"/>
            <corners android:radius="14dp"/>
        </shape>
    </item>

</layer-list>
