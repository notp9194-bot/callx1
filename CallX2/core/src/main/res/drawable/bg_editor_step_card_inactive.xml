<?xml version="1.0" encoding="utf-8"?>
<!-- Reel Editor step-card — NOT the current step (already-completed or
     not-yet-reached). Same rounded box as bg_editor_step_card_active but
     a flat subtle border instead of the brand gradient, so only the
     current step visually pops. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/trim_circle_btn_bg"/>
    <corners android:radius="16dp"/>
    <stroke
        android:width="1dp"
        android:color="@color/trim_divider"/>
</shape>
