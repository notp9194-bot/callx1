<?xml version="1.0" encoding="utf-8"?>
<!-- Poll feature (modernized) — vote-share fill bar. Radius matches the
     option row (14dp) so the fill never peeks past the rounded corners. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#3D5B5BF6"/>
    <corners android:radius="14dp"/>
</shape>
