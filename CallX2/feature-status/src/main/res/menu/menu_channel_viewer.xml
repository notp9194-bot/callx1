<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Search within channel -->
    <item
        android:id="@+id/action_channel_search"
        android:title="Search"
        android:icon="@drawable/ic_search"
        app:showAsAction="ifRoom"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <!-- Notification mute -->
    <item
        android:id="@+id/action_channel_mute"
        android:title="Mute notifications"
        app:showAsAction="never"/>

    <!-- Saved posts -->
    <item
        android:id="@+id/action_channel_highlights"
        android:title="Saved posts"
        app:showAsAction="never"/>

    <!-- Notification settings -->
    <item
        android:id="@+id/action_channel_notif_settings"
        android:title="Notification settings"
        app:showAsAction="never"/>

    <!-- Admin-only: Edit channel -->
    <item
        android:id="@+id/action_channel_edit"
        android:title="Edit channel"
        app:showAsAction="never"/>

    <!-- Admin-only: Admin panel -->
    <item
        android:id="@+id/action_channel_admin"
        android:title="Admin panel"
        app:showAsAction="never"/>

    <!-- Admin-only: Analytics -->
    <item
        android:id="@+id/action_channel_analytics"
        android:title="Analytics"
        app:showAsAction="never"/>

    <!-- Admin-only: Scheduled posts -->
    <item
        android:id="@+id/action_channel_scheduled"
        android:title="Scheduled posts"
        app:showAsAction="never"/>

    <!-- Admin-only: Broadcast -->
    <item
        android:id="@+id/action_channel_broadcast"
        android:title="Broadcast to followers"
        app:showAsAction="never"/>

    <!-- Follower: Unfollow -->
    <item
        android:id="@+id/action_channel_unfollow"
        android:title="Unfollow channel"
        app:showAsAction="never"/>

    <!-- Follower: Report -->
    <item
        android:id="@+id/action_channel_report"
        android:title="Report channel"
        app:showAsAction="never"/>

    <!-- Share channel link -->
    <item
        android:id="@+id/action_channel_share"
        android:title="Share channel"
        app:showAsAction="never"/>

</menu>
