<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Quick reactions row -->
    <LinearLayout
        android:id="@+id/layout_quick_reactions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="10dp" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#EEEEEE"
        android:layout_marginBottom="8dp" />

    <!-- Search emoji -->
    <EditText
        android:id="@+id/et_emoji_search"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:hint="Search emoji..."
        android:padding="6dp"
        android:textSize="13sp"
        android:background="@drawable/bg_search_input"
        android:layout_marginBottom="8dp" />

    <!-- Category tabs -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_emoji_categories"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        app:tabMode="scrollable"
        app:tabGravity="start"
        app:tabTextSize="12sp"
        android:layout_marginBottom="8dp" />

    <!-- Full emoji grid (filled programmatically) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="240dp">
        <LinearLayout
            android:id="@+id/layout_emoji_grid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

</LinearLayout>
