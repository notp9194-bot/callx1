<?xml version="1.0" encoding="utf-8"?>
<!--
  Small scrub-handle dot for the Reels seek bar — kept invisible (alpha set
  to 0 in code) until the user actually starts dragging, then faded in.
  Instagram's Reels progress line has no visible handle at rest either;
  showing one only while scrubbing keeps the idle look identical to before
  while still giving a clear grab-point once a drag starts.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#FFFFFFFF" />
    <size
        android:width="11dp"
        android:height="11dp" />
</shape>
