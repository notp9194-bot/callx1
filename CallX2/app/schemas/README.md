# Room schema files are auto-generated here on compile.
# Each file: schemas/com.callx.app.db.AppDatabase/<version>.json
# Commit these files to git so CI can diff schema changes before release.
# See AppDatabase.java for migration guide.
