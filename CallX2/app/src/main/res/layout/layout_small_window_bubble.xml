<?xml version="1.0" encoding="utf-8"?>
<!--
    layout_small_window_bubble.xml
    Minimized bubble — tap karo to restore small window.
    Screenshot 3 jaisa: corner mein chhota floating icon.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="56dp"
    android:layout_height="56dp"
    android:elevation="16dp">

    <!-- Circular background -->
    <androidx.cardview.widget.CardView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="center"
        app:cardCornerRadius="26dp"
        app:cardElevation="8dp"
        app:cardBackgroundColor="#1C1C1E">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_call_dark_scrim">

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_phone"
                android:tint="#FFFFFF"/>
        </FrameLayout>

    </androidx.cardview.widget.CardView>

    <!-- Blue dot — "active" indicator -->
    <View
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="2dp"
        android:layout_marginEnd="2dp"
        android:background="@drawable/circle_accept"/>

</FrameLayout>
