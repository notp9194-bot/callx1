<?xml version="1.0" encoding="utf-8"?>
<!-- Square grid cell using ConstraintLayout ratio -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/surface_input"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <!-- Square anchor view — forces 1:1 ratio -->
    <View
        android:id="@+id/view_ratio"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="1:1"/>

    <!-- Thumbnail fills the square -->
    <ImageView
        android:id="@+id/iv_thumb"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_gallery"
        app:layout_constraintTop_toTopOf="@id/view_ratio"
        app:layout_constraintBottom_toBottomOf="@id/view_ratio"
        app:layout_constraintStart_toStartOf="@id/view_ratio"
        app:layout_constraintEnd_toEndOf="@id/view_ratio"/>

    <!-- Play icon overlay — visible only for videos -->
    <ImageView
        android:id="@+id/iv_play_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_play"
        android:alpha="0.85"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/view_ratio"
        app:layout_constraintBottom_toBottomOf="@id/view_ratio"
        app:layout_constraintStart_toStartOf="@id/view_ratio"
        app:layout_constraintEnd_toEndOf="@id/view_ratio"/>

</androidx.constraintlayout.widget.ConstraintLayout>
