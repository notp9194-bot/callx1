<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Broadcast List — chat tab pe hi dikhega -->
    <item android:id="@+id/action_broadcast_list"
        android:title="📢 Broadcast List"
        app:showAsAction="never"/>

    <!-- v21: Delete All Chats — chat tab pe hi dikhega -->
    <item android:id="@+id/action_delete_all_chats"
        android:title="🗑️ Delete All Chats"
        app:showAsAction="never"/>

    <!-- Performance report — chat tab pe hi dikhega -->
    <item android:id="@+id/action_performance_report"
        android:title="⚡ Performance"
        app:showAsAction="never"/>

    <!-- Ultra Advanced Diagnostics — chat tab pe hi dikhega -->
    <item android:id="@+id/action_ultra_diagnostics"
        android:title="🔬 Ultra Advanced Diagnostics"
        app:showAsAction="never"/>

    <!-- v244: Settings — moved here from the toolbar avatar (avatar removed).
         Always visible, on every tab. -->
    <item android:id="@+id/action_settings"
        android:title="⚙️ Settings"
        app:showAsAction="never"/>

</menu>
