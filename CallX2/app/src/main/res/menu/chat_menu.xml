<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Toolbar action icons -->
    <item android:id="@+id/action_voice_call"
        android:title="Voice call"
        android:icon="@drawable/ic_phone"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/action_video_call"
        android:title="Video call"
        android:icon="@drawable/ic_video_call"
        app:showAsAction="ifRoom"/>

    <!-- Overflow menu items -->
    <!-- N3: View Profile -->
    <item android:id="@+id/action_view_profile"
        android:title="👤 View Profile"
        app:showAsAction="never"/>

    <!-- N6: Search in chat -->
    <item android:id="@+id/action_search"
        android:title="🔍 Search"
        app:showAsAction="never"/>

    <!-- Feature 7: Starred messages -->
    <item android:id="@+id/action_starred"
        android:title="⭐ Starred Messages"
        app:showAsAction="never"/>

    <!-- N8: Mute notifications (title toggled dynamically) -->
    <item android:id="@+id/action_mute"
        android:title="🔔 Mute"
        app:showAsAction="never"/>

    <!-- N8: Block user -->
    <item android:id="@+id/action_block"
        android:title="🚫 Block"
        app:showAsAction="never"/>

    <!-- N8: Clear local chat -->
    <item android:id="@+id/action_clear_chat"
        android:title="🗑 Clear Chat"
        app:showAsAction="never"/>

</menu>
