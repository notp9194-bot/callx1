<?xml version="1.0" encoding="utf-8"?>
<!-- Sent media bubble: neon cyan → electric lime gradient border via layer-list trick -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer layer = gradient border colour -->
    <item>
        <shape>
            <gradient android:type="linear" android:angle="135"
                android:startColor="#00F5FF" android:endColor="#AAFF00"/>
            <corners android:topLeftRadius="22dp" android:topRightRadius="22dp"
                android:bottomLeftRadius="22dp" android:bottomRightRadius="8dp"/>
        </shape>
    </item>
    <!-- Inner layer = inset by border width, fills with dark semi-transparent so image shows through -->
    <item android:left="3dp" android:top="3dp" android:right="3dp" android:bottom="3dp">
        <shape>
            <solid android:color="#CC000000"/>
            <corners android:topLeftRadius="20dp" android:topRightRadius="20dp"
                android:bottomLeftRadius="20dp" android:bottomRightRadius="6dp"/>
        </shape>
    </item>
</layer-list>
