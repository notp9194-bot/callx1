<?xml version="1.0" encoding="utf-8"?>
<!--
    item_view_all_reels_footer.xml
    Full-width footer cell shown at the end of the Liked/Saved reel grid
    (only when more than 6 reels exist) — tapping it opens the dedicated
    Instagram-style "View All Reels" scrolling screen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center"
    android:paddingTop="14dp"
    android:paddingBottom="14dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <TextView
        android:id="@+id/tv_view_all_reels"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View All Reels"
        android:textSize="14sp"
        android:textColor="@color/brand_primary"
        android:fontFamily="sans-serif-medium"/>

    <ImageView
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginStart="6dp"
        android:src="@drawable/ic_arrow_forward"
        android:tint="@color/brand_primary"
        android:contentDescription="View all"/>

</LinearLayout>
