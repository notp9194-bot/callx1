<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Full-screen video surface -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/player_view"
        android:saveEnabled="false"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="false"
        app:use_controller="false"
        app:resize_mode="zoom"
         app:surface_type="texture_view"/>

    <!-- ── Photo Slideshow ViewPager2 ───────────────────────────────────────
         Shown instead of PlayerView for photo_slideshow reels.
         Hidden (GONE) by default; shown only when mediaType == "photo_slideshow".
         User can also manually swipe between photos.
    -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/vp_photos"
        android:saveEnabled="false"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"/>

    <!-- Thumbnail placeholder (shown before video loads) -->
    <ImageView
        android:id="@+id/iv_thumb"
        android:saveEnabled="false"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="Reel thumbnail"/>

    <!-- ── Interactive sticker overlay layer (VIDEO reels) ──────────────────
         Renders reel.stickerJson (music/poll/quiz/countdown/slider/question/
         mention/hashtag/link) as live StatusStickerOverlayView widgets, same
         role as ll_sticker_layer in item_reel_photo_slide.xml for photo
         reels. Populated by ReelPlayerFragment.renderVideoStickers() —
         empty/GONE for reels with no stickers or for photo_slideshow reels
         (which render their stickers per-photo inside vp_photos instead).
    -->
    <FrameLayout
        android:id="@+id/fl_video_sticker_layer"
        android:saveEnabled="false"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"/>

    <!-- ── Story-style photo progress bar ───────────────────────────────────
         N thin animated segments at the very top, one per photo.
         Each active segment fills left→right over photoDurationMs ms.
         Populated programmatically by ReelPlayerFragment.buildStoryProgress().
         Visible only for photo_slideshow reels.
    -->
    <LinearLayout
        android:id="@+id/ll_story_progress"
        android:saveEnabled="false"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_gravity="top"
        android:layout_marginTop="14dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"/>

    <!-- Photo counter pill: "2 / 5" — centered at top, Instagram-style -->
    <TextView
        android:id="@+id/tv_photo_counter"
        android:saveEnabled="false"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="26dp"
        android:text="1 / 1"
        android:textColor="#FFFFFFFF"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/bg_photo_counter_pill"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:letterSpacing="0.05"
        android:visibility="gone"/>

    <!-- Pause overlay badge: ⏸ shown during long-press hold pause -->
    <TextView
        android:id="@+id/tv_pause_badge"
        android:layout_width="88dp"
        android:layout_height="88dp"
        android:layout_gravity="center"
        android:text="⏸"
        android:textSize="30sp"
        android:textColor="#EEFFFFFF"
        android:gravity="center"
        android:background="#77000000"
        android:alpha="0"
        android:visibility="gone"/>

    <!-- Per-photo caption overlay: slides up from bottom on photo change
         Modern frosted card with rounded corners, Instagram-style. -->
    <TextView
        android:id="@+id/tv_caption_overlay"
        android:saveEnabled="false"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="136dp"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="64dp"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:textColor="#FFFFFFFF"
        android:textSize="14sp"
        android:fontFamily="sans-serif"
        android:lineSpacingMultiplier="1.45"
        android:letterSpacing="0.01"
        android:background="@drawable/bg_photo_caption_card"
        android:alpha="0"
        android:translationY="20dp"
        android:visibility="gone"/>

    <!-- Instagram-style dot page indicator — pill active + circle inactive -->
    <LinearLayout
        android:id="@+id/ll_dot_indicator"
        android:saveEnabled="false"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="112dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"/>

    <!-- Double tap like animation -->
    <ImageView
        android:id="@+id/iv_like_anim"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_heart_filled"
        android:tint="#FF416C"
        android:visibility="gone"
        android:alpha="0"
        android:contentDescription="Like animation"/>

    <!-- ── Play / Pause center overlay ──
         Shown briefly on tap (animated in/out by ReelPlayerFragment).
         src is updated in Java to ic_play or ic_pause depending on state.
    -->
    <ImageView
        android:id="@+id/iv_play_pause_indicator"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_pause"
        android:tint="#A855F7"
        android:alpha="0"
        android:background="#66000000"
        android:padding="14dp"
        android:contentDescription="Play/Pause indicator"/>

    <!-- ── "Suggested for you" label + Follow button — shown for non-following reels ── -->
    <LinearLayout
        android:id="@+id/layout_suggested_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_reel_owner_top"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@username"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:fontFamily="sans-serif-medium"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Suggested for you"
                android:textColor="#CCCCCC"
                android:textSize="11sp"/>
        </LinearLayout>

        <TextView
            android:id="@+id/btn_reel_suggested_follow"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="Follow"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:background="@drawable/bg_reel_follow_btn"
            android:layout_marginEnd="8dp"
            android:clickable="true"
            android:focusable="true"/>
    </LinearLayout>

    <!-- ── End-of-reel overlay: "Watch more reels" + "Watch again" ── -->
    <LinearLayout
        android:id="@+id/layout_end_of_reel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="#88000000"
        android:visibility="gone">

        <!-- "Watch more reels" pill button -->
        <LinearLayout
            android:id="@+id/btn_watch_more_reels"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:background="#FFFFFFFF"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_play"
                android:tint="#000000"
                android:layout_marginEnd="10dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Watch more reels"
                android:textColor="#000000"
                android:textSize="15sp"
                android:fontFamily="sans-serif-medium"/>
        </LinearLayout>

        <!-- "Watch again" text -->
        <TextView
            android:id="@+id/btn_watch_again"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Watch again"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:layout_marginTop="16dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"/>
    </LinearLayout>

    <!-- Top row: mute button — paddingTop absorbs status-bar height (set in code).
         Speed (1x) and Download buttons removed from here — both actions
         already exist in the 3-dot "More" menu (ACTION_SPEED, ACTION_DOWNLOAD),
         no need to duplicate them up top. ReelPlayerController/ReelUiController
         already null-check btnSpeed/btnDownload before use, so removing these
         views here is safe. -->
    <LinearLayout
        android:id="@+id/top_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="horizontal"
        android:gravity="end"
        android:paddingTop="8dp"
        android:paddingEnd="12dp">

        <ImageButton
            android:id="@+id/btn_mute"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:src="@drawable/ic_volume_on"
            android:tint="#FFD700"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Mute"/>
    </LinearLayout>

    <!-- Right action bar: Avatar · Like · Comment · Share · More
         paddingBottom is set in code to bottomNavHeight + navBarHeight so actions
         stay above the bottom navigation bar on edge-to-edge screens.
    -->
    <!-- PERF: flattened from a nested vertical LinearLayout to a single-level
         ConstraintLayout (same pattern as the chat-bubble ConstraintLayout
         migration) — one measure/layout pass for the whole action rail
         instead of each child measuring inside its LinearLayout parent. -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/right_actions"
        android:layout_width="68dp"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:paddingBottom="8dp"
        android:paddingEnd="8dp"
        android:paddingStart="4dp">

        <!-- Like -->
        <ImageButton
            android:id="@+id/btn_like"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_heart"
            android:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Like"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>
        <TextView
            android:id="@+id/tv_likes_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginTop="7dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            app:layout_constraintTop_toBottomOf="@id/btn_like"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Comment -->
        <ImageButton
            android:id="@+id/btn_comment"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/ic_comment_reel"
            android:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Comment"
            app:layout_constraintTop_toBottomOf="@id/tv_likes_count"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>
        <TextView
            android:id="@+id/tv_comments_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginTop="7dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            app:layout_constraintTop_toBottomOf="@id/btn_comment"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Share -->
        <ImageButton
            android:id="@+id/btn_share"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/ic_share_reel"
            android:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Share"
            app:layout_constraintTop_toBottomOf="@id/tv_comments_count"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>
        <TextView
            android:id="@+id/tv_shares_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginTop="7dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            app:layout_constraintTop_toBottomOf="@id/btn_share"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Save -->
        <ImageButton
            android:id="@+id/btn_save"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/ic_bookmark"
            android:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Save"
            app:layout_constraintTop_toBottomOf="@id/tv_shares_count"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>
        <TextView
            android:id="@+id/tv_save_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Save"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginTop="7dp"
            app:layout_constraintTop_toBottomOf="@id/btn_save"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Repost -->
        <ImageButton
            android:id="@+id/btn_repost"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/ic_repost"
            android:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Repost"
            app:layout_constraintTop_toBottomOf="@id/tv_save_label"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>
        <TextView
            android:id="@+id/tv_repost_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginTop="7dp"
            app:layout_constraintTop_toBottomOf="@id/btn_repost"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Screenshot-style menu action: long line above, shorter line below.
             16dp gap to repost count above (matches the classic gap used
             between every icon group in this rail) and 16dp gap to the
             audio tile below it. -->
        <ImageButton
            android:id="@+id/btn_more"
            android:layout_width="42dp"
            android:layout_height="30dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/ic_reel_more_lines"
            android:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="More reel options"
            app:layout_constraintTop_toBottomOf="@id/tv_repost_count"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Audio cover tile: tapping it opens the existing sound/original-audio
             detail flow, not the audio recorder. -->
        <ImageButton
            android:id="@+id/btn_create_audio"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/ic_audio"
            android:scaleType="centerCrop"
            android:padding="2dp"
            android:background="@drawable/bg_audio_create_tile"
            android:contentDescription="Original audio"
            app:layout_constraintTop_toBottomOf="@id/btn_more"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Bottom info strip
         paddingBottom is set in code: navBarHeight only — strip sits at very bottom
    -->
    <!-- PERF: flattened from a nested vertical LinearLayout to a
         ConstraintLayout (same migration pattern used for right_actions and
         for the chat-bubble layouts) — the whole info strip resolves in one
         measure/layout pass instead of each row measuring inside its own
         LinearLayout parent. Internal groups (ll_owner_row's avatar stack,
         layout_music_ticker, reel_pinned_comment_container) keep their own
         shallow LinearLayout/FrameLayout since each is a single self-
         contained widget, not a chain — flattening those further wouldn't
         reduce nesting depth, just relocate it. -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/bottom_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:paddingStart="12dp"
        android:paddingEnd="72dp"
        android:paddingBottom="0dp">

        <!-- Owner avatar + name + follow button — hidden for collab posts,
             merged into ll_collab_second_author below instead (Instagram-style
             single-row collab display). -->
        <LinearLayout
            android:id="@+id/ll_owner_row"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="6dp">

            <!-- Owner avatar with story ring -->
            <FrameLayout
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_marginEnd="8dp">

                <ImageView
                    android:id="@+id/iv_owner_story_ring"
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:layout_gravity="center"
                    android:background="@drawable/story_ring_insta_gradient"
                    android:clickable="true"
                    android:focusable="true"
                    android:visibility="gone"/>

                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/iv_owner_avatar"
                    android:layout_width="34dp"
                    android:layout_height="34dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_person"
                    app:civ_border_width="1.5dp"
                    app:civ_border_color="@android:color/transparent"/>

                <TextView
                    android:id="@+id/btn_follow_overlay"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_gravity="bottom|end"
                    android:text="+"
                    android:textColor="#000000"
                    android:textSize="9sp"
                    android:gravity="center"
                    android:background="@color/brand_primary"
                    android:visibility="visible"/>
            
    <!-- ✨ Photo Style picker — visible only for photo_slideshow reels.
         ViewStub: not inflated until ReelUiController explicitly needs it,
         saving inflate cost on every other reel. -->
    <ViewStub
        android:id="@+id/stub_photo_style"
        android:inflatedId="@+id/btn_photo_style"
        android:layout="@layout/stub_reel_photo_style_btn"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="14dp"
        android:layout_marginBottom="190dp"/>

    <!-- 🎵 Beat-sync BPM indicator — shown when musicBpm > 0. ViewStub-backed. -->
    <ViewStub
        android:id="@+id/stub_bpm_badge"
        android:inflatedId="@+id/tv_bpm_badge"
        android:layout="@layout/stub_reel_bpm_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="228dp"/>

    <!-- Quality badge (ABR indicator: Auto / 360p / 720p etc) — optional, shown when AdaptiveStreamingManager is active. ViewStub-backed. -->
    <ViewStub
        android:id="@+id/stub_quality_badge"
        android:inflatedId="@+id/tv_quality_badge"
        android:layout="@layout/stub_reel_quality_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginTop="8dp"
        android:layout_marginStart="12dp"/>

</FrameLayout>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <TextView
                        android:id="@+id/tv_owner_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@user"
                        android:textColor="#FFFFFF"
                        android:textSize="15sp"
                        android:fontFamily="sans-serif-medium"/>
                    <TextView
                        android:id="@+id/tv_follow_btn"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="10dp"
                        android:text="Follow"
                        android:textColor="#00E5FF"
                        android:textSize="13sp"
                        android:paddingStart="8dp"
                        android:paddingEnd="8dp"
                        android:paddingTop="2dp"
                        android:paddingBottom="2dp"
                        android:background="@drawable/bg_reel_follow_btn"
                        android:visibility="visible"/>
                </LinearLayout>

                <!-- Instagram-style bio song-name row: small music note icon +
                     song name directly below the username line. Populated in
                     ReelUiController from the same musicDisplay string used
                     by the (now-hidden) bottom music ticker. -->
                <LinearLayout
                    android:id="@+id/ll_bio_song_row"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:clickable="true"
                    android:focusable="true"
                    android:background="?attr/selectableItemBackgroundBorderless">

                    <ImageView
                        android:layout_width="11dp"
                        android:layout_height="11dp"
                        android:src="@drawable/ic_music_note"
                        android:tint="#FFFFFF"
                        android:contentDescription="Song"/>

                    <TextView
                        android:id="@+id/tv_bio_song_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:maxWidth="180dp"
                        android:text="Original Audio"
                        android:textColor="#FFFFFF"
                        android:textSize="11sp"
                        android:maxLines="1"
                        android:ellipsize="end"/>
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>
        <!-- Caption -->
        <!-- DECLARED BEFORE stub_collab_row (below) on purpose: in a
             ConstraintLayout, later-declared children draw on top of
             earlier ones regardless of constraints. This is a defensive
             z-order safety net — the actual overlap fix is the dynamic
             constraint retarget in ReelUiController.retargetCaptionTopConstraint(),
             which repoints this view's top constraint to whichever of
             ll_owner_row / ll_collab_second_author is currently visible.
             That's the real fix; this ordering just guarantees the collab
             row is never visually hidden behind the caption even if the
             constraint retarget were ever skipped on some edge case. -->
        <TextView
            android:id="@+id/tv_caption"
        android:saveEnabled="false"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text=""
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            app:layout_constraintTop_toBottomOf="@id/ll_owner_row"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

          <!-- Collab Joint-Author row (shown only when isCollabPost=true) —
               ViewStub-backed: not inflated for the majority of (non-collab)
               reels. -->
          <ViewStub
              android:id="@+id/stub_collab_row"
              android:inflatedId="@+id/ll_collab_second_author"
              android:layout="@layout/stub_reel_collab_row"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_marginTop="6dp"
              app:layout_constraintTop_toBottomOf="@id/ll_owner_row"
              app:layout_constraintStart_toStartOf="parent"/>

        <!-- Mutual followers row — Instagram-style "Followed by X, Y and N
             others", same overlapping-avatar + text pattern as
             UserReelsActivity's layout_mutual_followers, adapted for the
             reel bio (dark video background instead of a surface card).
             Populated by ReelSocialController.loadMutualFollowers() against
             the reel OWNER's uid; hidden by default / when there are none. -->
        <LinearLayout
            android:id="@+id/layout_reel_mutual_followers"
            android:layout_width="wrap_content"
            android:layout_height="26dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="4dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/tv_caption"
            app:layout_constraintStart_toStartOf="parent">

            <FrameLayout
                android:id="@+id/frame_reel_mutual_avatars"
                android:layout_width="50dp"
                android:layout_height="22dp"
                android:layout_marginEnd="6dp">

                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/iv_reel_mutual_3"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center_vertical"
                    android:layout_marginStart="15dp"
                    android:src="@drawable/ic_person"
                    android:visibility="gone"
                    />

                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/iv_reel_mutual_2"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center_vertical"
                    android:layout_marginStart="7dp"
                    android:src="@drawable/ic_person"
                    android:visibility="gone"
                    />

                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/iv_reel_mutual_1"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center_vertical"
                    android:src="@drawable/ic_person"
                    android:visibility="gone"
                    />
            </FrameLayout>

            <TextView
                android:id="@+id/tv_reel_mutual_followers"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxWidth="200dp"
                android:textSize="11sp"
                android:textColor="#CCFFFFFF"
                android:maxLines="1"
                android:ellipsize="end"/>
        </LinearLayout>

        <!-- Hashtag chips -->
        <HorizontalScrollView
            android:id="@+id/scroll_hashtags"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            android:layout_marginTop="4dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/layout_reel_mutual_followers"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">
            <LinearLayout
                android:id="@+id/container_hashtags"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"/>
        </HorizontalScrollView>

        <!-- Animated Music Ticker -->
        <LinearLayout
            android:id="@+id/layout_music_ticker"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            
            android:paddingStart="8dp"
            android:paddingEnd="10dp"
            android:layout_marginTop="2dp"
            app:layout_constraintTop_toBottomOf="@id/scroll_hashtags"
            app:layout_constraintStart_toStartOf="parent">

            <ImageView
                android:id="@+id/iv_music_disc"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:src="@drawable/ic_music_disc"
                android:tint="#00E5FF"
                android:contentDescription="Music disc"/>

            <TextView
                android:id="@+id/tv_music_name"
                android:layout_width="140dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="Original Audio"
                android:textColor="#00E5FF"
                android:textSize="12sp"
                android:singleLine="true"
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever"
                android:scrollHorizontally="true"
                android:focusable="true"
                android:focusableInTouchMode="true"/>
        </LinearLayout>
        <!-- 📌 Pinned Comment Overlay -->
        <LinearLayout
            android:id="@+id/reel_pinned_comment_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:layout_marginHorizontal="8dp"
            android:background="@drawable/bg_pinned_comment_overlay"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="8dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/layout_music_ticker"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_pin"
                android:tint="#FFFFFF" />

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/reel_pinned_comment_avatar"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_person" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/reel_pinned_comment_author"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Author"
                    android:textColor="#FFFFFF"
                    android:textSize="12sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/reel_pinned_comment_text"
        android:saveEnabled="false"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="2"
                    android:text="Pinned comment content..."
                    android:textColor="#FFFFFF"
                    android:textSize="14sp" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:gravity="center"
                android:orientation="vertical">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_heart_filled"
                    android:tint="#FF416C" />

                <TextView
                    android:id="@+id/reel_pinned_comment_likes"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="0"
                    android:textColor="#FFFFFF"
                    android:textSize="10sp" />
            </LinearLayout>
        </LinearLayout>

        <!-- Live Reaction Counts -->
        <LinearLayout
            android:id="@+id/layout_live_reactions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="6dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/reel_pinned_comment_container"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>


    <!-- ── Floating Liker Avatars (Instagram explore style) ────────────────
         3 large circular avatars floating above bottom-left info strip.
         Gentle float animation applied in ReelPlayerFragment.
         visibility=gone until at least 1 liker loads.
         Tapping opens ReelLikesBottomSheet.
         NOTE: layout_marginBottom below is only the FIRST-FRAME fallback
         (before bottom_info has been measured once). ReelSocialController's
         attachDynamicLikerRowSpacing() overrides it at runtime to sit a
         fixed 16dp gap above bottom_info's real (possibly multi-row) height,
         so it never overlaps a taller caption/hashtag/music-ticker stack.
    -->
    <!-- outer wrapper: extra padding so float animation + hearts never clip -->
    <FrameLayout
        android:id="@+id/ll_likers_avatar_row"
        android:layout_width="140dp"
        android:layout_height="90dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="120dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:paddingTop="16dp"
        android:visibility="gone">

        <!-- Liker avatar 1 — left, bottom -->
        <FrameLayout
            android:id="@+id/fl_liker_1"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_gravity="bottom|start"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless">

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/iv_liker_1"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_gravity="top|start"
                android:src="@drawable/ic_person"
                app:civ_border_width="1.5dp"
                app:civ_border_color="#FF416C"
                android:elevation="2dp"
                android:visibility="gone"/>

            <TextView
                android:id="@+id/tv_heart_1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:text="&#x2764;&#xFE0F;"
                android:textSize="14sp"
                android:elevation="4dp"
                android:visibility="gone"/>
        </FrameLayout>

        <!-- Liker avatar 2 — center, top -->
        <FrameLayout
            android:id="@+id/fl_liker_2"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="top|center_horizontal"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless">

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/iv_liker_2"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_gravity="top|start"
                android:src="@drawable/ic_person"
                app:civ_border_width="1.5dp"
                app:civ_border_color="#A855F7"
                android:elevation="3dp"
                android:visibility="gone"/>

            <TextView
                android:id="@+id/tv_heart_2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:text="&#x2764;&#xFE0F;"
                android:textSize="14sp"
                android:elevation="4dp"
                android:visibility="gone"/>
        </FrameLayout>

        <!-- Liker avatar 3 — right, bottom -->
        <FrameLayout
            android:id="@+id/fl_liker_3"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_gravity="bottom|end"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless">

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/iv_liker_3"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_gravity="top|start"
                android:src="@drawable/ic_person"
                app:civ_border_width="1.5dp"
                app:civ_border_color="#00E5FF"
                android:elevation="2dp"
                android:visibility="gone"/>

            <TextView
                android:id="@+id/tv_heart_3"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:text="&#x2764;&#xFE0F;"
                android:textSize="14sp"
                android:elevation="4dp"
                android:visibility="gone"/>
        </FrameLayout>

    </FrameLayout>

    <!-- Video progress bar — pinned to very bottom of video (0dp margin).
         Instagram-style drag-to-seek: a SeekBar instead of a plain
         ProgressBar. The 28dp view height is just the touch/drag target;
         progress_reel_seekbar.xml paints the actual ~3dp line flush with
         the very bottom edge of that target, and always visible (not
         faded) so the user can see where it is without dragging first.
         Only the thumb starts invisible (alpha 0, set in code) and fades
         in while actively scrubbing — see ReelPlayerController's seek bar
         wiring. -->
    <SeekBar
        android:id="@+id/progress_video"
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:layout_gravity="bottom"
        android:max="1000"
        android:progress="0"
        android:paddingStart="0dp"
        android:paddingEnd="0dp"
        android:splitTrack="false"
        android:progressDrawable="@drawable/progress_reel_seekbar"
        android:thumb="@drawable/thumb_reel_seek"
        android:layout_marginBottom="0dp"/>

    <!-- Buffering indicator -->
    <ProgressBar
        android:id="@+id/progress_buffering"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:indeterminateTint="#A855F7"
        android:visibility="gone"/>

    <!-- Emoji reaction panel -->
    <LinearLayout
        android:id="@+id/layout_reactions"
        android:layout_width="wrap_content"
        android:layout_height="52dp"
        android:layout_gravity="end|center_vertical"
        android:layout_marginEnd="60dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:background="@drawable/bg_reaction_panel"
        android:elevation="10dp"
        android:visibility="gone">

        <TextView android:id="@+id/react_fire"  android:layout_width="40dp" android:layout_height="40dp" android:text="🔥" android:textSize="22sp" android:gravity="center"/>
        <TextView android:id="@+id/react_heart" android:layout_width="40dp" android:layout_height="40dp" android:text="❤️" android:textSize="22sp" android:gravity="center"/>
        <TextView android:id="@+id/react_wow"   android:layout_width="40dp" android:layout_height="40dp" android:text="😮" android:textSize="22sp" android:gravity="center"/>
        <TextView android:id="@+id/react_laugh" android:layout_width="40dp" android:layout_height="40dp" android:text="😂" android:textSize="22sp" android:gravity="center"/>
        <TextView android:id="@+id/react_sad"   android:layout_width="40dp" android:layout_height="40dp" android:text="😢" android:textSize="22sp" android:gravity="center"/>
        <TextView android:id="@+id/react_clap"  android:layout_width="40dp" android:layout_height="40dp" android:text="👏" android:textSize="22sp" android:gravity="center"/>
    </LinearLayout>


    <!-- ── REPOST ATTRIBUTION BANNER v4.2.9 ─────────────────────────────────────
         Shown at the top when viewing a reposted reel.
         Java: ReelPlayerFragment binds tv_repost_attribution + ll_repost_attribution
         visibility=gone by default; made VISIBLE only for reposted reels.
    -->
    <LinearLayout
        android:id="@+id/ll_repost_attribution"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:layout_marginTop="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#CC000000"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="&#x1F501;"
            android:textSize="14sp"
            android:layout_marginEnd="8dp"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_repost_attribution"
        android:saveEnabled="false"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Reposted from @creator"
                android:textColor="#00F260"
                android:textSize="13sp"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:ellipsize="end"/>

            <TextView
                android:id="@+id/tv_repost_caption_preview"
        android:saveEnabled="false"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#CE93D8"
                android:textSize="11sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:visibility="gone"/>
        </LinearLayout>

        <TextView
            android:id="@+id/btn_view_original"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="View original"
            android:textColor="#4CAF50"
            android:textSize="12sp"
            android:paddingStart="12dp"
            android:fontFamily="sans-serif-medium"/>
    </LinearLayout>

    <!-- ── REPOST COUNT CHIP v4.2.9 ──────────────────────────────────────────────
         Shows "🔁 N reposts" bottom-left for the reel owner.
         Tapping opens ReelRepostListActivity to see who reposted.
    -->
    <LinearLayout
        android:id="@+id/ll_repost_count_chip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="96dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#CC1A1A1A"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="&#x1F501;"
            android:textSize="13sp"
            android:layout_marginEnd="4dp"/>

        <TextView
            android:id="@+id/tv_repost_count_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 reposts"
            android:textColor="#A855F7"
            android:textSize="12sp"/>
    </LinearLayout>

      <!-- ── DUET SERIES CHIP ────────────────────────────────────────────────
           Shown when reel.seriesId != null.
           • Tap  → opens DuetSeriesActivity for this series
           • Long → instant subscribe / unsubscribe toast + Firebase toggle
      -->
      <LinearLayout
          android:id="@+id/ll_series_chip"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_gravity="bottom|start"
          android:layout_marginStart="12dp"
          android:layout_marginBottom="138dp"
          android:orientation="horizontal"
          android:gravity="center_vertical"
          android:background="#CC1A1A1A"
          android:paddingStart="10dp"
          android:paddingEnd="10dp"
          android:paddingTop="6dp"
          android:paddingBottom="6dp"
          android:visibility="gone">

          <TextView
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:text="🎬"
              android:textSize="14sp"
              android:layout_marginEnd="5dp"/>

          <TextView
              android:id="@+id/tv_series_chip_label"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:textColor="#FFFFFF"
              android:textSize="12sp"
              android:fontFamily="sans-serif-medium"
              android:maxLines="1"
              android:ellipsize="end"
              android:maxWidth="200dp"/>

          <TextView
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:text="  ›"
              android:textColor="#AAFFFFFF"
              android:textSize="12sp"/>
      </LinearLayout>

    <!-- Instagram-style floating reaction replay — plays once when this
         reel has received emoji reactions. Purely visual, never
         intercepts touch. See ReelSocialController#loadLiveReactionCounts(). -->
    <com.callx.app.reaction.FloatingReactionOverlayView
        android:id="@+id/reel_reaction_burst_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"/>

  
</FrameLayout>
