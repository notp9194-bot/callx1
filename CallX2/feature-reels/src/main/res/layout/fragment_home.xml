<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh_home"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- v243: replaced NestedScrollView+LinearLayout with a single
             top-level RecyclerView. The old layout eagerly inflated every
             feed card as a permanent LinearLayout child (never recycled),
             so an unbounded paginated feed meant an unbounded number of
             live views/ExoPlayer-adjacent bitmaps forever. Header (stories
             + toggle) and the trailing sections (trending/suggested/
             activity/continue-watching) are now item view types 0 and
             last, rendered from item_home_header.xml / item_home_footer.xml
             — everything in between (posts + interleaved suggested rows)
             gets real ViewHolder recycling. See HomeFragment's FeedAdapter. -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_home"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="none"
            android:clipToPadding="false"/>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
