<?xml version="1.0" encoding="utf-8"?>
<!-- ✅ NEW: small number badge overlapping the bottom edge of each step
     icon box — always solid brand pink regardless of step progress (only
     the box border + label color change with active/completed/future
     state, per the screenshot reference). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/step_badge_bg"/>
</shape>
