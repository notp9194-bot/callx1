<?xml version="1.0" encoding="utf-8"?>
<!-- ✅ NEW: Step 2 · Text Overlay icon for the icon-box stepper — a simple
     bold "T" glyph, matching the crossbar + stem look used in the screenshot
     reference instead of a generic text-lines icon. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">

    <!-- Crossbar -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="2.4"
        android:strokeLineCap="round"
        android:pathData="M5,6 L19,6"/>

    <!-- Stem -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="2.4"
        android:strokeLineCap="round"
        android:pathData="M12,6 L12,18"/>
</vector>
