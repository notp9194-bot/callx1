<?xml version="1.0" encoding="utf-8"?>
<!--
    Original Creator row — thin-outline card (transparent fill, 1dp border)
    instead of the solid gray box (bg_sound_detail_box), matching the
    screenshot's bordered-card look for this row specifically.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/>
    <stroke android:width="1dp" android:color="@color/sound_detail_button_light"/>
    <corners android:radius="12dp"/>
</shape>
