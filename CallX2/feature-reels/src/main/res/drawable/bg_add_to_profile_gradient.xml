<?xml version="1.0" encoding="utf-8"?>
<!--
    "Add to profile" CTA — solid pink→orange gradient pill, matching the
    screenshot's filled accent button (the one solid-colored action among
    the three outlined-vs-filled use-sound buttons).
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="45"
        android:startColor="@color/sound_cta_gradient_start"
        android:endColor="@color/sound_cta_gradient_end"/>
    <corners android:radius="15dp"/>
</shape>
