<?xml version="1.0" encoding="utf-8"?>
<!-- Popup shown from the consolidated input-row "+" button, replacing the
     old always-visible Attach / Camera / View-Once trio with a single
     on-demand menu. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/menu_plus_attach"
        android:title="@string/plus_menu_attach" />
    <item
        android:id="@+id/menu_plus_camera"
        android:title="@string/plus_menu_camera" />
    <item
        android:id="@+id/menu_plus_view_once"
        android:title="@string/plus_menu_view_once" />
</menu>
