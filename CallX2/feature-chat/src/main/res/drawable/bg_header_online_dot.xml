<?xml version="1.0" encoding="utf-8"?>
<!-- Green online dot with a ring matching the flat iOS-style header
     background, so it looks inset into the avatar (see screenshot ref). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/status_online"/>
    <stroke
        android:width="2dp"
        android:color="@color/bar_background"/>
</shape>
