<?xml version="1.0" encoding="utf-8"?>
<!--
    Subtle 2-color diagonal gradient for the floating header capsule
    (Telegram/iOS-style premium look). Reuses existing brand_gradient_*
    tokens so it matches the app's day/night theme automatically —
    no per-screen recoloring needed.

    Drawn as a plain rectangle behind cv_header_capsule's content;
    MaterialCardView clips it to the card's rounded-corner outline,
    so no corner radius is needed here.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="135"
        android:startColor="@color/brand_gradient_start"
        android:endColor="@color/brand_gradient_end"/>
</shape>
