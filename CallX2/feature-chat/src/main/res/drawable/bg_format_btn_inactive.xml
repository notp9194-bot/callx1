<?xml version="1.0" encoding="utf-8"?>
<!-- v169 — Advanced format toolbar button background (inactive/default state).
     Sibling active state should be bg_format_btn_active if a "selected" style
     is added later (e.g. for btnFmtAlign's current alignment). -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#1F1976D2">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#00000000"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>
</ripple>
