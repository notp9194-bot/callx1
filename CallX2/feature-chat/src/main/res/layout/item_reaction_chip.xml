<?xml version="1.0" encoding="utf-8"?>
<!--
  item_reaction_chip.xml
  Single emoji reaction chip shown in the reaction row below a message.

  States:
    Normal:  white pill bg, grey count
    Active (currentUserReacted): brand_primary tinted bg + border

  Inflate one per ReactionSummary and add to item_reaction_bubble.xml's reaction_row.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/chip_root"
    android:layout_width="wrap_content"
    android:layout_height="24dp"
    android:orientation="horizontal"
    android:gravity="center"
    android:paddingStart="6dp"
    android:paddingEnd="6dp"
    android:layout_marginEnd="4dp"
    android:background="@drawable/bg_reaction_chip"
    android:clickable="true"
    android:focusable="true">

    <!-- Emoji -->
    <TextView
        android:id="@+id/tv_emoji"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:includeFontPadding="false"/>

    <!-- Count (hidden when count == 1) -->
    <TextView
        android:id="@+id/tv_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#555555"
        android:textSize="11sp"
        android:textStyle="bold"
        android:layout_marginStart="3dp"
        android:includeFontPadding="false"/>

</LinearLayout>
