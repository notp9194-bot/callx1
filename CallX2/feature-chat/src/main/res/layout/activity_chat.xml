<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/chat_unified_bg">
    <!-- FrameLayout wraps wallpaper + shimmer + RecyclerView + floating bars + FAB -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Chat Wallpaper Background Image -->
        <ImageView
            android:id="@+id/iv_chat_wallpaper"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone"
            android:contentDescription="Chat wallpaper"/>

        <!-- Shimmer skeleton -->
        <com.facebook.shimmer.ShimmerFrameLayout
            android:id="@+id/shimmer_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone">
            <include layout="@layout/layout_skeleton_chat"/>
        </com.facebook.shimmer.ShimmerFrameLayout>

        <!-- Messages list -->
        <com.callx.app.chat.performance.FastFlingRecyclerView
            android:id="@+id/rv_messages"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:paddingTop="68dp"
            android:paddingBottom="76dp"
            android:clipToPadding="false"
            android:visibility="gone"/>

        <!-- FIX [P3-4]: Empty chat state — no messages yet.
             PERF: lazily inflated via ViewStub — only needed the first time
             a chat is confirmed to have zero messages (see
             layout_empty_chat_state.xml); most chat opens never hit this,
             so it no longer inflates 3 TextViews on every screen open. -->
        <ViewStub
            android:id="@+id/stub_empty_chat"
            android:inflatedId="@+id/ll_empty_chat"
            android:layout="@layout/layout_empty_chat_state"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <!-- Screenshot banner.
             PERF: lazily inflated via ViewStub — see
             layout_screenshot_banner.xml. Most chat sessions never see a
             screenshot event. -->
        <ViewStub
            android:id="@+id/stub_screenshot_banner"
            android:inflatedId="@+id/ll_screenshot_banner"
            android:layout="@layout/layout_screenshot_banner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <!-- ══════════════════════════════════════════════════════════════
             PARTNER / MEMBERS "WATCHING" BANNER — appears when someone
             currently has THIS chat screen open &amp; foregrounded. Avatar(s)
             zoom in 0dp → 48dp, name(s) shown below in a vibrant highlight
             strip. Does NOT touch the normal online/last-seen status line.
             Floats ON TOP of the message list (instead of its own row above
             it) so the chat area behind it stays full height — messages
             don't get squeezed when the banner pops in/out.

             1:1 chats (ChatPresenceController) only ever touch
             iv_watching_avatar + tv_watching_name — iv_watching_avatar2 and
             tv_watching_more stay gone, so the banner looks identical to
             before. Group chats (GroupWatchingController) additionally use
             iv_watching_avatar2 (2nd watcher, overlapping) and
             tv_watching_more ("+N" badge) for 3+ watchers, e.g.
             "Asha, Ravi aur 2 others dekh rahe hain".
             ══════════════════════════════════════════════════════════════ -->
        <LinearLayout
            android:id="@+id/ll_watching_banner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|center_horizontal"
            android:layout_marginTop="68dp"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:foreground="?attr/selectableItemBackground"
            android:visibility="gone">

            <LinearLayout
                android:id="@+id/ll_watching_avatars"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/iv_watching_avatar"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_person"
                    app:civ_border_width="2dp"
                    app:civ_border_color="#22C55E"/>

                <!-- Group only: 2nd watcher's avatar, overlaps 1st -->
                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/iv_watching_avatar2"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginStart="-14dp"
                    android:src="@drawable/ic_person"
                    android:visibility="gone"
                    app:civ_border_width="2dp"
                    app:civ_border_color="#22C55E"/>

                <!-- Group only: "+N" badge when 3+ members are watching -->
                <TextView
                    android:id="@+id/tv_watching_more"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginStart="-14dp"
                    android:background="@drawable/bg_watching_more_badge"
                    android:gravity="center"
                    android:textColor="#FFFFFF"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:visibility="gone"/>

            </LinearLayout>

            <TextView
                android:id="@+id/tv_watching_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:background="@drawable/bg_watching_strip"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:paddingTop="5dp"
                android:paddingBottom="5dp"
                android:text="aapko dekh rha hai"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold"/>

        </LinearLayout>

        <!-- ══════════════════════════════════════════════════════════════
             TYPING STRIP — floats bottom-left over the message list while
             the partner (1:1) or any member (group) is typing. Same vibrant
             gradient pill language as ll_watching_banner above, but anchored
             bottom|start instead of top|center so it never collides with it,
             and carries its own avatar + animated 3-dot indicator instead of
             reusing the toolbar's old plain-text tv_typing line.

             1:1 chats: iv_typing_avatar + tv_typing_name show the partner.
             Group chats: same views, but tv_typing_name renders either
             "Asha typing…" (1 typer) or "Asha, Ravi typing…" (2+, names
             joined — GroupTypingController truncates/pluralizes for 3+).
             ══════════════════════════════════════════════════════════════ -->
        <!-- Canvas-rendered replacement for the old LinearLayout(avatar +
             name TextView + 3-child-View dot row). ONE view now paints the
             avatar, name and the 3 bouncing dots directly — see
             TypingStripCanvasView for why (real bounce animation restored,
             without the per-frame multi-View invalidate cost that made the
             old TypingDotsAnimator get turned into a no-op). -->
        <com.callx.app.conversation.canvas.TypingStripCanvasView
            android:id="@+id/ll_typing_strip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="76dp"
            android:background="@drawable/bg_watching_strip"
            android:paddingStart="6dp"
            android:paddingEnd="14dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:visibility="gone"/>

        <!-- ══════════════════════════════════════════════════════════════
             VOICE RECORDING INDICATOR — "Rahul is recording a voice
             message 🎙️" — the audio-version of the typing strip. Shows
             when the partner is actively holding the mic button and
             recording a voice note. Uses the same pill style as
             ll_typing_strip but with a pulsing mic icon + waveform bar
             instead of the three bouncing dots.
             Driven by ChatPresenceController#watchPartnerRecording.
             ══════════════════════════════════════════════════════════════ -->
        <!-- Voice recording indicator (partner recording a voice note).
             PERF: lazily inflated via ViewStub — see
             layout_voice_recording_strip.xml. Most chat sessions never
             see the partner recording. -->
        <ViewStub
            android:id="@+id/stub_voice_recording_strip"
            android:inflatedId="@+id/ll_voice_recording_strip"
            android:layout="@layout/layout_voice_recording_strip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <!-- ══════════════════════════════════════════════════════════════
             LIVE TYPING PREVIEW BOX — bottom-left, shows the partner's
             in-progress message text EXACTLY as they type/delete it,
             character by character, before they hit send. Sits just above
             the input bar so it never overlaps ll_typing_strip's avatar
             pill (that one shows WHO is typing; this one shows WHAT).
             Driven by ChatLiveTypingController. Rendered by
             LiveTypingCanvasView — ONE view painting both the "typing
             live" label and the wrapped draft text directly via Canvas,
             replacing what used to be a LinearLayout + two child TextViews
             re-measuring/re-drawing on every single keystroke the partner
             makes. ══════════════════════════ -->
        <com.callx.app.conversation.canvas.LiveTypingCanvasView
            android:id="@+id/ll_live_typing_preview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="70dp"
            android:layout_marginBottom="126dp"
            android:background="@drawable/bg_watching_strip"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:clickable="false"
            android:focusable="false"
            android:visibility="gone"/>

        <!-- ══════════════════════════════════════════════════════════════
             EMOJI BURST OVERLAY — when the partner sends an emoji-only
             message, it pops up HUGE in the center, overlapping the whole
             chat area, with a scale+fade animation, then auto-vanishes after
             a short delay. Driven by ChatEmojiBurstController, which guards
             against the chat-open backlog replay, ignores our own messages,
             and enforces a 4-second cooldown so the partner can't spam it.
             Rendered by EmojiBurstCanvasView — a plain Canvas draw, not a
             TextView, so changing the emoji only ever calls invalidate(),
             never requestLayout(). match_parent + independent FrameLayout
             sibling of the RecyclerView means it never competes with the
             message list's own measure/layout/scroll passes — scrolling
             stays buttery smooth. Ignores touches so it never blocks the
             chat list.
             PERF: stays visibility="visible" + alpha="0" at rest, and
             ChatEmojiBurstController never calls setVisibility()/GONE —
             only animates alpha. Toggling a View to/from GONE requests a
             fresh measure+layout pass on the parent (a well-known Android
             cost, unlike INVISIBLE/alpha which only repaints), so the
             previous gone/visible toggle queued an extra Choreographer
             layout pass on every single burst show AND hide — competing
             with RecyclerView scroll/fling frames for the same 16ms
             budget, exactly the class of jank this view was built to
             avoid. Alpha-only show/hide costs a GPU compositing pass, no
             layout at all. ─ -->
        <com.callx.app.conversation.canvas.EmojiBurstCanvasView
            android:id="@+id/tv_emoji_burst"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="false"
            android:focusable="false"
            android:visibility="visible"
            android:alpha="0"/>

        <!-- New Messages floating indicator chip (WhatsApp style).
             Appears above the FAB when user is scrolled up and new messages arrive. -->
        <TextView
            android:id="@+id/tv_new_messages_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="128dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingTop="7dp"
            android:paddingBottom="7dp"
            android:text="↓ new messages"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold"
            android:background="@drawable/bg_new_msg_chip"
            android:elevation="6dp"
            android:clickable="true"
            android:focusable="true"
            android:visibility="gone"/>

        <!-- Back to latest FAB — shown when navigating to original message -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_back_to_latest"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginStart="12dp"
            android:layout_marginTop="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginBottom="84dp"
            android:src="@drawable/ic_arrow_back"
            android:rotation="270"
            android:contentDescription="Back to latest"
            app:backgroundTint="@color/brand_primary"
            app:tint="@color/white"
            app:fabSize="mini"
            android:visibility="gone"
            android:alpha="0"/>

        <!-- ═══════════════════════════════════════════════════════════════
             FLOATING BOTTOM STACK — sits INSIDE the chat-area FrameLayout so
             the capsule input bar floats on top of the message list (modern
             UI) instead of pushing it up from below.
             ═══════════════════════════════════════════════════════════════ -->
        <LinearLayout
            android:id="@+id/ll_floating_bottom_stack"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical">

            <ProgressBar
                android:id="@+id/upload_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:indeterminate="true"
                android:progressTint="@color/brand_primary"
                android:visibility="gone"/>

            <!-- ═══════════════════════════════════════════════════════════
                 REPLY BAR — Upgraded: media thumbnail + animated slide-up
                 ═══════════════════════════════════════════════════════════ -->
            <LinearLayout
                android:id="@+id/ll_reply_bar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:background="@drawable/bg_reply_bar"
                android:paddingStart="12dp"
                android:paddingEnd="8dp"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:layout_marginStart="10dp"
                android:layout_marginEnd="10dp"
                android:gravity="center_vertical"
                android:visibility="gone">

                <!-- Left accent stripe -->
                <View
                    android:id="@+id/view_reply_accent"
                    android:layout_width="3dp"
                    android:layout_height="44dp"
                    android:background="@color/brand_primary"
                    android:layout_marginEnd="10dp"/>

                <!-- Media thumbnail (image/video preview) -->
                <ImageView
                    android:id="@+id/iv_reply_bar_thumb"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:scaleType="centerCrop"
                    android:layout_marginEnd="8dp"
                    android:background="@color/divider"
                    android:visibility="gone"/>

                <!-- Sender name + content text -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">
                    <TextView
                        android:id="@+id/tv_reply_bar_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/brand_primary"
                        android:textSize="12sp"
                        android:textStyle="bold"
                        android:maxLines="1"
                        android:ellipsize="end"/>
                    <TextView
                        android:id="@+id/tv_reply_bar_text"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/text_secondary"
                        android:textStyle="italic"
                        android:textSize="12sp"
                        android:maxLines="2"
                        android:ellipsize="end"/>
                </LinearLayout>

                <!-- Cancel button -->
                <ImageButton
                    android:id="@+id/btn_cancel_reply"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:src="@drawable/ic_back"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Cancel reply"/>
            </LinearLayout>

            <!-- Smart link preview — WhatsApp/Telegram-style: user URL type
                 karte hi (debounced) OG title/thumbnail card yahan dikhta
                 hai, send se pehle hi. Same LinkPreviewFetcher jo already
                 sent-message bubbles ke liye use hota hai. -->
            <LinearLayout
                android:id="@+id/ll_link_preview_bar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:background="@drawable/bg_reply_bar"
                android:paddingStart="12dp"
                android:paddingEnd="8dp"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:layout_marginStart="10dp"
                android:layout_marginEnd="10dp"
                android:layout_marginTop="6dp"
                android:gravity="center_vertical"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/iv_link_preview_thumb"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:scaleType="centerCrop"
                    android:layout_marginEnd="8dp"
                    android:background="@color/divider"
                    android:visibility="gone"/>

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">
                    <TextView
                        android:id="@+id/tv_link_preview_domain"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/brand_primary"
                        android:textSize="11sp"
                        android:textStyle="bold"
                        android:maxLines="1"
                        android:ellipsize="end"/>
                    <TextView
                        android:id="@+id/tv_link_preview_title"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/text_secondary"
                        android:textSize="12sp"
                        android:maxLines="2"
                        android:ellipsize="end"/>
                </LinearLayout>

                <ImageButton
                    android:id="@+id/btn_cancel_link_preview"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:src="@drawable/ic_back"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Dismiss link preview"/>
            </LinearLayout>

            <!-- Block banner — shown when YOU have blocked this person -->
            <LinearLayout
                android:id="@+id/ll_block_banner"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:background="#1AFF3B30"
                android:paddingStart="16dp"
                android:paddingEnd="12dp"
                android:paddingTop="10dp"
                android:paddingBottom="10dp"
                android:layout_marginStart="10dp"
                android:layout_marginEnd="10dp"
                android:gravity="center_vertical"
                android:visibility="gone">

                <TextView
                    android:id="@+id/tv_block_banner_text"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textColor="#FFFF3B30"
                    android:textSize="13sp"
                    android:drawablePadding="8dp"
                    android:text="You have blocked this person"/>

                <Button
                    android:id="@+id/btn_permanent_block"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:text="Permanent Block"
                    android:textSize="12sp"
                    android:textColor="#FFFFFFFF"
                    android:backgroundTint="#FFFF3B30"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:layout_marginStart="8dp"
                    style="@style/Widget.MaterialComponents.Button"/>

            </LinearLayout>

            <!-- ═══════════════════════════════════════════════════════════
                 FLOATING CAPSULE INPUT BAR — pill shape, subtle elevation,
                 floats above the bottom safe-area over the chat area.
                 ═══════════════════════════════════════════════════════════ -->

            <!-- ═══════════════════════════════════════════════════════════
                 @MENTION SUGGESTION LIST — slides in above the input capsule
                 when the user types "@" in the message field.
                 Shown by ChatMentionController / GroupMentionController.
                 visibility=gone by default; controller shows/hides it.
                 ═══════════════════════════════════════════════════════════ -->
            <!--
                @MENTION SUGGESTION LIST — slides in above the input capsule when the
                user types "@" in the message field.  The MaterialCardView provides
                rounded corners + elevation; it collapses to 0 height automatically
                when its only child (rv_mention_suggest) is GONE (wrap_content).
                Controllers only manage rv_mention_suggest visibility.
            -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_marginEnd="10dp"
                android:layout_marginBottom="4dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="8dp"
                app:cardBackgroundColor="@color/surface_input"
                app:strokeColor="@color/divider"
                app:strokeWidth="0.5dp"
                xmlns:app="http://schemas.android.com/apk/res-auto">

                <!--
                    visibility="gone" here — controllers call
                    binding.rvMentionSuggest.setVisibility(VISIBLE/GONE).
                    When GONE, the parent card collapses to 0 height (wrap_content).
                -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rv_mention_suggest"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:maxHeight="192dp"
                    android:overScrollMode="never"
                    android:nestedScrollingEnabled="false"
                    android:clipToPadding="false"
                    android:visibility="gone"/>

            </com.google.android.material.card.MaterialCardView>

            <!-- ═══════════════════════════════════════════════════════════
                 WhatsApp-style input row: the pill (cv_input_capsule) holds
                 view-once + text field + attach/camera + mic/send, all
                 INSIDE the one pill (mic/send moved back in — no more
                 separate floating round button beside it). Pill background
                 stays @color/surface_input, no green tint (see Theme.CallX's
                 colorSurfaceTint fix) — only the mic/send button itself is
                 green (circle_primary), same as WhatsApp's own send button.
                 ═══════════════════════════════════════════════════════════ -->
            <!-- Character count indicator — sirf tab dikhta hai jab
                 maxLength ke paas (200 chars ya kam bache) ho, WhatsApp/
                 Twitter-style counter. Limit paar hone par red ho jata hai. -->
            <TextView
                android:id="@+id/tv_char_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginEnd="14dp"
                android:layout_marginTop="2dp"
                android:textSize="11sp"
                android:textColor="@color/text_muted"
                android:visibility="gone"/>

            <LinearLayout
                android:id="@+id/ll_input_bar_row"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="10dp"
                android:orientation="horizontal"
                android:gravity="bottom"
                android:clipChildren="false"
                android:clipToPadding="false">

                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/cv_input_capsule"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="10dp"
                    android:layout_marginEnd="10dp"
                    app:cardCornerRadius="28dp"
                    app:cardElevation="0dp"
                    app:cardBackgroundColor="@color/chat_input_bar_bg"
                    app:strokeColor="@color/divider"
                    app:strokeWidth="0.5dp"
                    app:cardPreventCornerOverlap="false"
                    app:cardUseCompatPadding="false">

                    <!-- Input row — view-once toggle on the left, message
                         field in the middle, attach+camera grouped on the
                         right, all inside the pill (WhatsApp layout).

                         PERF (ultra-opt pass v12): converted from a nested-
                         weight LinearLayout to ConstraintLayout. The old
                         LinearLayout had a single weighted child (et_message)
                         so it was already single-pass, but it still sat
                         inside ll_input_bar_row -> cv_input_capsule, two
                         more weight/measure-bearing ancestors above it —
                         every one of those participates in the same
                         measure/layout pass whenever the capsule resizes
                         (keyboard show/hide, line-count change). A flat
                         ConstraintLayout resolves all six children's
                         positions in one pass via constraints instead of
                         LinearLayout's sequential child measurement, which
                         matters most here since this row re-measures on
                         every capsule height change. -->
                    <androidx.constraintlayout.widget.ConstraintLayout
                        android:id="@+id/ll_input_row"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:paddingStart="4dp"
                        android:paddingEnd="4dp"
                        android:paddingTop="4dp"
                        android:paddingBottom="4dp"
                        android:background="@android:color/transparent">

                        <ImageButton
                            android:id="@+id/btn_view_once"
                            android:layout_width="34dp"
                            android:layout_height="34dp"
                            android:padding="6dp"
                            android:contentDescription="View Once"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            app:layout_constraintStart_toStartOf="parent"
                            app:layout_constraintTop_toTopOf="parent"
                            app:layout_constraintBottom_toBottomOf="parent"/>

                        <!-- PERF (ultra-opt pass): et_message used to sit inside a
                             FrameLayout that existed only to hold its weight/
                             margins/padding — one extra ViewGroup measured and
                             laid out on every relayout of this row (i.e. on
                             every text change that grows/shrinks the pill).
                             Removed; the same weight/margin/padding now live
                             directly on the EditText, same visual result. -->
                        <com.callx.app.chat.ui.GifAwareEditText
                            android:id="@+id/et_message"
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="4dp"
                            android:layout_marginEnd="4dp"
                            android:hint="@string/hint_message"
                            android:textColorHint="@color/text_muted"
                            android:textColor="@color/chat_input_text"
                            android:background="@color/transparent"
                            android:paddingTop="5dp"
                            android:paddingBottom="5dp"
                            android:paddingStart="10dp"
                            android:paddingEnd="10dp"
                            android:textSize="17sp"
                            android:maxLines="4"
                            android:maxLength="4000"
                            app:layout_constraintStart_toEndOf="@id/btn_view_once"
                            app:layout_constraintEnd_toStartOf="@id/chat_icon_bar"
                            app:layout_constraintTop_toTopOf="parent"
                            app:layout_constraintBottom_toBottomOf="parent"/>

                        <!-- PERF (icon-bar merge pass): btn_attach, btn_camera,
                             btn_mic and btn_send used to be four separate
                             ImageButtons here (four measure/layout passes,
                             four background drawables, and a per-frame
                             setLayoutParams() animation on attach+camera
                             every time they collapsed/expanded around typed
                             text). Replaced with one Canvas-drawn view — see
                             ChatIconBarView for the full rationale. Mic
                             drag-to-lock/cancel geometry code still reads
                             this view's live on-screen position via
                             getLocationInWindow() + getMicBoundsInView(),
                             so it keeps working correctly from here. -->
                        <com.callx.app.chat.ui.ChatIconBarView
                            android:id="@+id/chat_icon_bar"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:contentDescription="Attach, camera, voice message and send"
                            app:layout_constraintStart_toEndOf="@id/et_message"
                            app:layout_constraintEnd_toEndOf="parent"
                            app:layout_constraintTop_toTopOf="parent"
                            app:layout_constraintBottom_toBottomOf="parent"/>
                    </androidx.constraintlayout.widget.ConstraintLayout>

                    <!-- ═══════════════════════════════════════════════════════
                         RECORDING BAR — replaces ll_input_row while a voice
                         message is being held/locked (see
                         layout_recording_bar.xml for the full view tree and
                         PERF rationale). Lazily inflated via ViewStub on
                         first mic-press instead of always inflating with
                         this screen — saves 8 views' worth of inflate +
                         measure on every chat open, since most sessions
                         never record a voice message.
                         android:inflatedId keeps the same ll_recording_bar
                         id the rest of the codebase already references once
                         inflated. ═══════════════════════════════════════ -->
                    <ViewStub
                        android:id="@+id/stub_recording_bar"
                        android:inflatedId="@+id/ll_recording_bar"
                        android:layout="@layout/layout_recording_bar"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"/>
                </com.google.android.material.card.MaterialCardView>

            </LinearLayout>

        </LinearLayout>

        <!-- ═══════════════════════════════════════════════════════════════
             SLIDE-TO-LOCK bubble — sibling of the floating bottom stack (NOT
             inside it) so it can float freely ABOVE the capsule, overlapping
             the mic button, instead of stacking below it. Positioned via
             gravity like fab_scroll_down above. Hidden until recording
             starts; drag finger up past the threshold to lock hands-free
             (icon flips open → closed padlock).
             ═══════════════════════════════════════════════════════════════ -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cv_record_lock"
            android:layout_width="40dp"
            android:layout_height="112dp"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="72dp"
            app:cardCornerRadius="20dp"
            app:cardElevation="4dp"
            app:cardBackgroundColor="@color/surface_input"
            app:strokeColor="@color/divider"
            app:strokeWidth="0.5dp"
            android:visibility="gone"
            android:alpha="0">

            <ImageView
                android:id="@+id/iv_record_lock_icon"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="top|center_horizontal"
                android:layout_marginTop="12dp"
                android:src="@drawable/ic_record_lock_open"
                app:tint="@color/brand_primary"
                android:contentDescription="Slide up to lock"/>
        </com.google.android.material.card.MaterialCardView>

    </FrameLayout>

    <!-- ═══════════════════════════════════════════════════════════════
         FLOATING TOP STACK — sits ABOVE the message FrameLayout as an
         overlay so the header floats over the chat area (same pattern
         as the bottom floating input stack).
         ═══════════════════════════════════════════════════════════════ -->
    <LinearLayout
        android:id="@+id/ll_floating_top_stack"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="vertical">

        <!-- ═══════════════════════════════════════════════════════════
             iOS-STYLE HEADER — flat nav bar, no card/rounded corners/
             elevation, sits flush with the top safe-area (see reference
             screenshot). cv_header_capsule removed; id "toolbar" kept
             as-is since nothing else in this layout references the old
             card id.
             ═══════════════════════════════════════════════════════════ -->
        <LinearLayout
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/chat_unified_bg"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:paddingStart="8dp"
            android:paddingEnd="12dp">

                <!-- Back button — iOS chevron, no circular ripple/tail bar -->
                <ImageButton
                    android:id="@+id/btn_back"
                    android:layout_width="30dp"
                    android:layout_height="30dp"
                    android:src="@drawable/ic_chevron_back_ios"
                    android:tint="@color/text_primary"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Back"/>

                <!-- Avatar + online dot (clickable to open profile) -->
                <FrameLayout
                    android:layout_width="34dp"
                    android:layout_height="34dp"
                    android:layout_marginStart="2dp">

                    <de.hdodenhof.circleimageview.CircleImageView
                        android:id="@+id/iv_partner_avatar"
                        android:layout_width="34dp"
                        android:layout_height="34dp"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_person"
                        android:clickable="true"
                        android:focusable="true"/>
                </FrameLayout>

                <!-- Name + status/typing (fills remaining space) -->
                <LinearLayout
                    android:id="@+id/ll_partner_name_row"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:layout_marginStart="10dp"
                    android:layout_marginEnd="4dp"
                    android:clickable="true"
                    android:focusable="true">

                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <TextView
                            android:id="@+id/tv_partner_name"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="Contact Name"
                            android:textColor="@color/text_primary"
                            android:textSize="18sp"
                            android:textStyle="bold"
                            android:maxLines="1"
                            android:ellipsize="end"/>

                        <ImageView
                            android:id="@+id/iv_partner_name_verified"
                            android:layout_width="13dp"
                            android:layout_height="13dp"
                            android:layout_marginStart="4dp"
                            android:src="@drawable/ic_verified_pink"
                            android:visibility="gone"
                            android:contentDescription="Verified"/>
                    </LinearLayout>

                    <!-- Online / last-seen status only — typing no longer shown here,
                         see ll_typing_strip floating banner over the message list. -->
                    <TextView
                        android:id="@+id/tv_status"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/text_secondary"
                        android:textSize="11sp"
                        android:layout_marginTop="1dp"
                        android:visibility="gone"/>
                </LinearLayout>

                <!-- Reel button — patli rassi se latakta hua, hawa mein jhulta -->
                <!-- Pura container: rassi + button ek saath upar se neeche -->
                <LinearLayout
                    android:id="@+id/ll_reel_hanging"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="center_horizontal"
                    android:layout_marginEnd="3dp"
                    android:translationY="-4dp"
                    android:visibility="gone">

                    <!-- Patli rassi (thin string from top of header) -->
                    <View
                        android:id="@+id/view_reel_thread"
                        android:layout_width="1.5dp"
                        android:layout_height="8dp"
                        android:background="@drawable/ic_reel_thread"
                        android:layout_gravity="center_horizontal"/>

                    <!-- Reel button itself — jhulta hua.
                         PERF: GONE by default — was VISIBLE from inflate,
                         so it drew its default drawable for one frame before
                         partner data even loaded, then a second draw pass
                         once we knew whether the partner actually has a
                         reel/X/YouTube profile. Revealed in ChatActivity
                         once that check comes back true; stays GONE (saving
                         a draw pass every time this screen opens) when the
                         partner has no linked profile for that feature. -->
                    <ImageButton
                        android:id="@+id/btn_toolbar_reel"
                        android:layout_width="30dp"
                        android:layout_height="30dp"
                        android:src="@drawable/ic_reels"
                        android:tint="@color/white"
                        android:background="@drawable/bg_social_reel_btn"
                        android:padding="6dp"
                        android:visibility="gone"
                        android:contentDescription="View Reels Profile"/>

                </LinearLayout>

                <!-- X button — GONE by default, see note above ll_reel_hanging. -->
                <ImageButton
                    android:id="@+id/btn_toolbar_x"
                    android:layout_width="30dp"
                    android:layout_height="30dp"
                    android:src="@drawable/ic_x_logo"
                    android:tint="@color/white"
                    android:background="@drawable/bg_social_x_btn"
                    android:padding="6dp"
                    android:layout_marginEnd="3dp"
                    android:visibility="gone"
                    android:contentDescription="View X Profile"/>

                <!-- YouTube button — GONE by default, see note above ll_reel_hanging. -->
                <ImageButton
                    android:id="@+id/btn_toolbar_youtube"
                    android:layout_width="30dp"
                    android:layout_height="30dp"
                    android:src="@drawable/ic_youtube"
                    android:tint="@color/white"
                    android:background="@drawable/bg_social_youtube_btn"
                    android:padding="6dp"
                    android:layout_marginEnd="1dp"
                    android:visibility="gone"
                    android:contentDescription="View YouTube Channel"/>

                <!-- Voice call button -->
                <ImageButton
                    android:id="@+id/btn_toolbar_voice_call"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_marginStart="4dp"
                    android:src="@drawable/ic_phone"
                    android:tint="@color/text_primary"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:padding="10dp"
                    android:contentDescription="Voice call"/>

                <!-- Video call button -->
                <ImageButton
                    android:id="@+id/btn_toolbar_video_call"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_marginStart="6dp"
                    android:src="@drawable/ic_video_call"
                    android:tint="@color/text_primary"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:padding="9dp"
                    android:contentDescription="Video call"/>

                <!-- More options (3-dot menu) -->
                <ImageButton
                    android:id="@+id/btn_more_options"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_marginStart="6dp"
                    android:src="@drawable/ic_more_vert"
                    android:tint="@color/text_primary"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:padding="9dp"
                    android:contentDescription="More options"/>

        </LinearLayout>

        <!-- ══════════════════════════════════════════════════════════════
             INSTAGRAM-STYLE PROFILE CARD — big avatar + stats + Subscribe +
             View Community. See ChatProfileCardBinder for all binding logic.
             Collapsed by default; expands on tapping the header name row.
             ══════════════════════════════════════════════════════════════ -->
        <include
            android:id="@+id/include_profile_card"
            layout="@layout/layout_chat_profile_card"/>

        <!-- ══════════════════════════════════════════════════════════════
             MULTI-SELECT TOOLBAR (WhatsApp style).
             PERF: lazily inflated via ViewStub — see
             layout_selection_toolbar.xml. Most chat sessions never enter
             multi-select, so these 7 views no longer inflate on every
             chat open.
             ══════════════════════════════════════════════════════════════ -->
        <ViewStub
            android:id="@+id/stub_selection_toolbar"
            android:inflatedId="@+id/ll_selection_toolbar"
            android:layout="@layout/layout_selection_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Offline banner.
             PERF: lazily inflated via ViewStub — see
             layout_offline_banner.xml. Only needed while actually
             offline. -->
        <ViewStub
            android:id="@+id/stub_offline_banner"
            android:inflatedId="@+id/tv_offline_banner"
            android:layout="@layout/layout_offline_banner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Search bar.
             PERF: lazily inflated via ViewStub — see
             layout_search_bar.xml. Most chat opens never use search. -->
        <ViewStub
            android:id="@+id/stub_search_bar"
            android:inflatedId="@+id/ll_search_bar"
            android:layout="@layout/layout_search_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Pinned message banner.
             PERF: lazily inflated via ViewStub — see
             layout_pinned_banner.xml. Only needed once a message is
             actually pinned; most chats never pin anything. -->
        <ViewStub
            android:id="@+id/stub_pinned_banner"
            android:inflatedId="@+id/ll_pinned_banner"
            android:layout="@layout/layout_pinned_banner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Scheduled messages banner — shows count of pending scheduled sends.
             PERF: lazily inflated via ViewStub — see
             layout_scheduled_banner.xml. Only needed once a message is
             actually scheduled; most chats never use scheduled send. -->
        <ViewStub
            android:id="@+id/stub_scheduled_banner"
            android:inflatedId="@+id/ll_scheduled_banner"
            android:layout="@layout/layout_scheduled_banner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>

    <!-- ══════════════════════════════════════════════════════════════════
         STICKY DATE HEADER — Telegram/WhatsApp style. Floats over the top
         of rv_messages (NOT inside ll_floating_top_stack — that stack pushes
         its siblings down, this needs to overlay on top of the message list
         instead), shows whichever date is currently scrolled to, and is kept
         hidden/faded by ChatActivity#setupStickyDateHeader() except while
         actively scrolling. Root FrameLayout gravity positions it as a plain
         overlay, same pattern as fab_back_to_latest / cv_record_lock above.
         ══════════════════════════════════════════════════════════════════ -->
    <TextView
        android:id="@+id/tv_sticky_date_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="76dp"
        android:background="@drawable/bg_date_chip"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:textColor="#FFFFFFFF"
        android:textSize="12sp"
        android:textStyle="bold"
        android:elevation="4dp"
        android:alpha="0"
        android:visibility="gone"
        android:text="Today"/>

</FrameLayout>
